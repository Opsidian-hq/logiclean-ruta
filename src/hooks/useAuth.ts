/**
 * Logiclean Ruta — useAuth hook
 *
 * Facade sobre AuthContext para uso en componentes.
 * Re-exporta el valor del contexto con nombre más corto.
 */

export { useAuthContext as useAuth } from '../context/AuthContext';
